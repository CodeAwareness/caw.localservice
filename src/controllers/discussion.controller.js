/* @flow */

const httpStatus = require('http-status')
const catchAsync = require('@/utils/catchAsync')

const getDiscussions: any = catchAsync(async (req, res) => {
})

const comment: any = catchAsync(async (req, res) => {
})

module.exports = {
  comment,
  getDiscussions,
}
