/* eslint-disable @typescript-eslint/no-empty-function */
const setupTestDB = () => {
  beforeAll(async () => {
  })

  beforeEach(async () => {
  })

  afterAll(async () => {
  })
}

module.exports = setupTestDB
